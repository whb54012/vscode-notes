sudo 命令 将当前命令以管理员模式运行
visudo/vim etc/sudoers 修改使用sudo用户权限
su - 用户名 切换到指定用户,直接用su默认切换到root权限
#group
groupadd 组名 创建一个用户组
groupdel 组名 删除指定用户组
#user
useradd -g whb -d /home/whb -m 用户名
-g(指定放进的用户组)
-d(指定的/home路径)
-m(指定路径不存在时自动创建创建,后面没有参数)
不使用-g的情况下会自动创建和用户名同名用户组,如果已存在同名组,必须用-g,否则无法创建
userdel -r(加上后将用户目录也一并删除) 用户名 删除指定用户
usermod -aG 用户组 用户名 将用户追加到新用户组里面
gpasswd -d 用户名 用户组 将指定的一个用户组移除
usermod -g 用户组 用户名 修改主组
#passwd
passwd 用户名 给新用户设置密码
#id
id 用户名 查看指定用户所属组,不写就默认当前使用用户
#chmod 修改文件对于不同用户的权限
chmod -R u=rwx,g=r--,o=r-- 文件或文件夹
chmod -R 111(每个数字位代表各自权限) 文件或文件夹(简便写法)
r=1,w=2,rw=3,x=4,rx=5,wx=6,rwx=7
-R表示对文件或文件夹及其内部设置同样的权限
u=user所属当前用户权限
g=group所属组用户权限
o=other其他用户权限
chmod u+w/u-w/u=w 文件或文件夹 对当前用户进行指定文件/文件夹权限加减或覆盖
#chown 修改文件所属的主用户及其所属组
chown -R(文件夹及其内部所有文件) 用户名 :用户组 文件/文件夹
#先创建组,无需任何参数,再创建用户,创建用户需要考虑他的组,属于他的文件或文件夹
,是否需要手动创建文件夹功能#