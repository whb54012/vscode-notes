分号;   顺序执行多条语句
管道符|     将前面执行语句的结果当作后面语句的输入
逻辑或||    前面命令执行失败才执行后面命令
逻辑与&&    前面命令执行成功才执行后面命令
换行符%0a(\n)   命令中穿插,写在命令和参数之间可以当空格,在末尾可以当命令分隔符
回车符%0d(\r)   等效于%0a
tab符%09(\t)    替换空格
${IFS}          替换空格
后台运行符&     多条命令同时执行,后台内容穿插回显
注释符#     注释后面的语句
反引号``    将引号里面的内容当命令执行
命令替换$()     和反引号功能一样
tac:    反向的cat,将结果由最后一行向第一行读取
head:   默认输出文章前十行,可自己调整行数
    head -n 输出行数 文件名
tail:   默认输出文章后十行,可自己调整行数
    tail -n 输出行数 文件名
rev:    将内容反向输出
more/less:  分页查找,如果是非终端则是全部显示,和cat类似
*通配符(可占据0或多个字符):匹配当前目录所有文件或目录,可在前后加上内容匹配符合内容的文件/目录
        cat *_flag.php:匹配所有尾部是_flag.php的文件,成功读取falg.php文件
        cat fl*ag.php:绕过flag检测,成功读取falg.php文件
        cat /e*/p*wd:匹配所有符合类似的文件,绕过检测成功读取到etc/passwd隐私文件
        用于绕过有文件名检测的防护
?通配符(只占据一个位置):匹配当前目录所有和?个数相同的文件或目录,可在前后加上内容匹配符合内容的文件/目录
        cat ?.txt:匹配文件名只有一个的txt文件,如a,txt,b.txt,c.txt
        cat ????.php:可通过叠加?个数来匹配,????.php能成功找到falg.php
        cat /e??/p???wd:匹配到个数相同的etc/passwd隐私文件