hostnamectl: 系统信息
cat /proc/version：版本信息
uname -a：系统信息,架构、OS等
exec 命令：替换当前进程
find / -name 文件名 -exec '命令文件路径或者命令名(命令名走path路径,文件路径更保险)' \;
读取到一个文件就开一个子进程执行命令
命令被引号包裹的作用是对特殊字符如空格,管道符之类的进行生效：
find / -name 文件名 -exec 'cat 文件' \;
find / -name 文件名 -exec ls \;没有空格可以不用引号包裹
exec结束符：
\;找到一个文件开一个进程
{} +找到文件后不管文件多少只开一个

