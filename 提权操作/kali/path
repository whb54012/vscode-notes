1.通过suid或有权限的定时任务中找到会调用path路径命令的脚本或程序
2.创建一个同名的命令，将其添加进path路径，让脚本或程序在手动或自动调用时以root
权限执行假的命令文件达成提权

1.系统任务或者拥有suid的脚本程序,内部指向调用一个命令文件
                        |
    find / -perm -4000 -type f 2>/dev/null                        
2.在tmp文件夹中创建一个相同命令文件,并添加进$PATH路径。如果路径被禁,
就用.来动态写入$PATH路径表示从当前所在路径中找取命令文件
                        |
export PATH=/tmp/ls:$PATH  export PATH=.:$PATH  