元组,列表,字典都可多维,集合不可,内部元素个数也不要求一致
多维情况下前两着者可以随意穿插,如:
my_tuple=[(1,2,3),[1,3],[4]]
my_tuple[1][1]--> 3
字典可以进入前两者内部,但取值时必须使用键而非数字,如:
list=[(1,2),{'姓名':'whb'}]
list[1]['姓名']--> whb
字典内部只能塞入元组,因为元组不可修改,哈希值不变
