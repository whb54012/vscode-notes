# 损失函数:衡量每个样本预测值和真实值效果的函数
# 减低损失函数方法:
    # 1.正规函数法
    # 2.梯度下降法
# 最小二乘法:通过将每个预测值和真实值的平方和加起来比较出k和b
# 均方误差:通过将每个预测值和真实值的平方和加起来平分求出k和b
# LinearRegression()默认使用最小二层法求出损失函数最低的偏置和权重
# ∑(i=0->m)Xw-B=0
# ->X(矩阵)w-B(矩阵)
# ->XXᵀw=BXXᵀ
# ->(XᵀX)⁻¹ (XᵀX) w = (XᵀX)⁻¹ Xᵀy
# ->I · w = (XᵀX)⁻¹ Xᵀy
# -> w  = (XᵀX)⁻¹ Xᵀy