1. 客户端回显漏洞（验证码回显）
原理：后端将真实短信验证码明文写在 HTTP 响应包（Response）中返回前端，前端 JS 隐藏不展
示，但抓包查看返回报文就能直接拿到验证码。Burp 操作：提交手机号获取验证码→查看Response
数据包，直接提取验证码填写登录 / 改密，不用查看手机短信。成因:开发调试遗留，验证码下发后
未从返回数据剔除。

2. Response 返回值篡改绕过（你之前学习的改返回包漏洞）
原理：验证码校验逻辑只在前端依据后端返回状态判断后端改密接口不二次核验验证码。Burp操作
：填写错误验证码提交，拦截 Response，把{"code":0,"msg":"错误"}修改为{"code":1,"msg":
"验证通过"}后放行，前端直接进入改密流程。

3. 验证码爆破漏洞
原理：验证码位数短（4/6 位纯数字）、无错误次数锁定、无 IP 限流、无图形验证码防护。
Burp 操作：intruder 模块批量遍历验证码字典，循环发包，根据 Response 长度 / 状态码区分
正确验证码。

4. 找回流程绕过（越权绕过验证码校验）
两种实现方式：
直构造改密数据包：验证码校验与密码修改拆分两个接口，改密接口不校验验证码，抓到正常改密请
求后，直接重放数据包跳过验证码步骤修改密码。删除 / 篡改参数绕过：剔除请求内captcha验证
码字段、参数置空，后端无空值拦截，直接放行改密。

(如果对于验证通过后,后端不会对其改密包中请求的手机号验证而直接通过,又者先获得正确的响应
包,再填写他人账号和错误验证码发起请求,将返回的错误响应包改成正确的响应包,就可能篡改他人
账户而存在越权漏洞)