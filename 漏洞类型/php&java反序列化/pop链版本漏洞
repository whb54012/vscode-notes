php版本在5到5.624和7到7.09之间对于__wakeup的绕过方法
当pop链序列化后如果表示属性的个数大于实际的属性个数就会不执行
__wakeup方法:
这里的三如果大于后面的实际属性个数就会绕过
             |
O:6:"person":3:{s:4:"name";s:6:"参数";s:11:"personage";i:18;s:4:"*a";i:1;}

字符串逃逸
通过后端字符串替换为长度长的或短的进行不同绕过
原理:通过反序列化时编译器根据属性的参数来进行读取值,如果后端改变了值的内容大小,那便通过
利用属性的参数大小与实际的值内容大小的出入使其挤出恶意代码让翻译器将其当作参数,又或者
吞噬后面的参数使其绕过强制赋值
列如:
O:3:"per":2:{s:4:"name";s:3:"属性值";s:3:"age";i:18;}
我在属性值写入whb";s:3:"age";i:20;},他便根据s:3只读取前面3个,从而将我写入的恶意参数
";s:3:"age";i:20;}当成对象被其编译,而后面的参数责备丢弃,从而达成修改参数的结果

字符串缩短吞噬也是同样的道理,跟据缩短位数给后面的参数多添加几位然后注入恶意代码,把正常
代码挤出解析器范围,让他解析恶意代码
$s=O:3:"per":2:{s:4:"name";s:6:"whbwhb";s:3:"age";i:18;}
$s=str.replace('whb','wh',$s);
将s:6:构造成"whbwhbwhb";s:3:"age";i:18;}
绕过匹配

方法不止以上两种,根据实际情况去选择采取不同方案
