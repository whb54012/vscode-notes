通过找到waf与后端服务器不同的分割点与截断点来绕过waf又不会使后端出现在后缀名等地方分割来实现

数据溢出绕过:在检验的内容前如Content-Disposition、name、filename之间塞入大量垃圾数据
并用;分割使waf无法扫描到后面的,成功进入后端

防止符号匹配绕过:修改或调整数据包中包裹数据的引号和分号,使waf匹配时无法找到准确数据被绕过,
而服务器匹配数据包会根据解析器的宽松程度而可能保存不同的文件名,也可能失败

%00,换行等截断符绕过:有些waf用C语言写的内核,会读取结束符从截断(1.ph%00p>1.ph),这样就匹配不
到非法格式php,如果后端代码也会触发截断,那就通过其他方法(1.php.%00.jpg或者这里直接用.php.
绕过对php字符串检验,或者添加如$data,空之类不影响的其他字符),实际情况看waf是否严格

参数重复匹配绕过:对要检验的参数复制塞入大量相同的数据,在最后在写上需要绕过的参数,绕过waf检测成
功进入后端